# CLAUDE.md

This file points coding agents at the canonical project context.

**Read [`AGENTS.md`](AGENTS.md)** for what VortexFlow is, the tech stack, local
dev (`make` targets), the pre-commit checks, and the key conventions/footguns.
That is the single source of truth for both humans and AI agents.

## Local working context

Detailed, internal working notes (current release status, phase history, design
decisions in flight) live in `CLAUDE.local.md`, which is **gitignored** and only
present in a maintainer's working tree — it is intentionally not part of the
public repository.

@CLAUDE.local.md
